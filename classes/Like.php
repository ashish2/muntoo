<?php

/*
 * Like table is an object, 
 * Like object will be passed, 
 * Post(whichever it is, wall_post(wp_id), wall_post_reply(wpr_id)
 * and User_id,
 * and it will return whether this post is still liked by this user.
 * 
 * */


//$objects = array()
//$objects['post']
//$objects['user']


// Defining class Like
class Like($objects)
{
	// Defining all Columns of Like Object(table)
	// Columns of like table
	//id
	//post_id
	//user_id
	
}


?>
